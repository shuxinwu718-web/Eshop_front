# Eshop 电商系统 — 毕设项目整改报告

> 文档版本：v1.1 ｜ 整改时间：2026-08-06 ~ 08-07 ｜ 状态：已整改完成并通过验证
> 用途：本报告为毕业设计「系统测试与问题整改」章节的核心支撑材料，记录系统开发与审查过程中发现的**高危问题、修复方案与验证结果**。
> 关联文档：[代码质量改进文档](./代码质量改进文档.md)、[生产问题解决文档](./生产问题解决文档.md)

---

## 目录

1. [整改概述](#1-整改概述)
2. [整改问题总览](#2-整改问题总览)
3. [安全类问题整改](#3-安全类问题整改)
4. [并发与数据一致性整改](#4-并发与数据一致性整改)
5. [前端全局缺陷整改](#5-前端全局缺陷整改)
6. [本次整改新增内容](#6-本次整改新增内容)
7. [测试与验证结果](#7-测试与验证结果)
8. [整改前后对比](#8-整改前后对比)
9. [遗留问题与后续计划](#9-遗留问题与后续计划)
10. [结论](#10-结论)

---

## 1. 整改概述

本系统为前后端分离电商平台：后端 **Spring Boot 3.2 + Spring Security + MyBatis-Plus + MySQL + Redis + Elasticsearch**，前端 **Vue 3 + TypeScript + Vite + Element-Plus**。

整改工作基于**全量代码审查、安全扫描与并发推演**发现的 19 项问题（其中高危 13 项），按照「安全 > 数据一致性 > 前端可用性」的优先级完成修复。整改完成后通过**单元测试 + 接口实测 + 构建验证**三层手段确认系统功能正常、风险已消除。

**整改原则**：
- 最小改动：针对根因修改，不重构无关代码；
- 原子校验：并发场景统一采用「条件 UPDATE + 受影响行数判断」，不引入分布式锁等复杂方案；
- 验证闭环：每项修复均通过编译、测试或接口实测确认。

---

## 2. 整改问题总览

| 编号 | 问题描述 | 等级 | 整改状态 |
|------|----------|------|----------|
| S1 | 方法级安全未启用，`@PreAuthorize` 全部失效 | 🔴 高危 | ✅ 已整改 |
| S2 | 管理端接口无角色控制 | 🔴 高危 | ✅ 已整改 |
| S3 | 优惠券越权使用（不校验归属） | 🔴 高危 | ✅ 已整改 |
| S4 | 匿名文件上传 + 无类型白名单（存储型 XSS） | 🔴 高危 | ✅ 已整改 |
| S5 | 敏感信息硬编码（SMTP 授权码 / JWT 密钥） | 🔴 高危 | ✅ 已整改 |
| S6 | 密码重置验证码删除键错误，可无限复用 | 🔴 高危 | ✅ 已整改 |
| S7 | 登录安全缺陷：日志泄露完整 JWT + 图形验证码可绕过 | 🔴 高危 | ✅ 已整改 |
| S8 | 已注销用户可被搜索出（SQL 优先级错误） | 🟠 中危 | ✅ 已整改 |
| C1 | 下单扣库存非原子，存在并发超卖 | 🔴 高危 | ✅ 已整改 |
| C2 | 优惠券领取超发 | 🔴 高危 | ✅ 已整改 |
| C3 | 秒杀 DB 库存丢更新 | 🟠 中危 | ✅ 已整改 |
| C4 | 定时任务自调用导致事务失效 | 🟠 中危 | ✅ 已整改 |
| F1 | 前端全屏 loading 永久泄漏 | 🔴 高危 | ✅ 已整改 |
| F2 | Axios 封装类型错位，`request` 不可调用 | 🔴 高危 | ✅ 已整改 |
| F3 | 页面绕过 API 层裸调 request，类型重复 | 🟠 中危 | ✅ 已整改 |
| R1 | 我的优惠券页调用未定义函数（运行时白屏） | 🔴 高危 | ✅ 已整改 |
| R2 | 领券中心接口类型字段缺失 | 🟡 低危 | ✅ 已整改 |
| N1 | mapper XML namespace 双重包名（9 个文件） | 🔴 高危 | ✅ 本次整改 |
| N2 | 参数校验不完整（数量/价格/库存无范围限制） | 🔴 高危 | ✅ 本次整改 |
| N3 | 越权访问返回 500 而非 403（异常兜底掩盖） | 🟠 中危 | ✅ 本次整改 |
| N4 | 测试覆盖为空 | 🟠 中危 | ✅ 本次整改 |
| Q7 | 秒杀状态魔数散落（0/1/2 直接写死） | 🟡 低危 | ✅ 本次整改 |
| Q9 | 购物车列表 N+1 查询 | 🟡 低危 | ✅ 本次整改 |
| Q11 | TokenUtils 死代码 | 🟡 低危 | ✅ 本次整改 |
| Q12 | BCryptPasswordEncoder 重复实例化 | 🟡 低危 | ✅ 本次整改 |
| Q13 | 操作日志 type 拼写/命名混乱（24 处） | 🟡 低危 | ✅ 本次整改 |

---

## 3. 安全类问题整改

### 3.1 S1 方法级安全未启用，`@PreAuthorize` 全部失效

**问题描述**：启动类缺少 `@EnableMethodSecurity`（Spring Security 6 起不再默认开启），导致全部 18 处方法级权限注解不生效，任何已登录用户均可调用本应受限的接口。

**修复方案**：

```java
@EnableScheduling
@EnableMethodSecurity   // 新增
@SpringBootApplication
public class EShopApplication { ... }
```

**验证结果**：`EShopApplication.java` 已确认添加注解；接口实测普通用户访问管理接口返回 **HTTP 403**。

### 3.2 S2 管理端接口无角色控制

**问题描述**：`AdminSeckillController`、`AdminCouponController`、`AdminRefundController`、`AdminMerchantApplyController` 等无任何权限注解，任意注册用户可直接创建秒杀、发券、审核退款。

**修复方案**：在开启方法级安全基础上，为各管理 Controller 补齐类级/方法级 `@PreAuthorize("hasRole('ADMIN')")`；退款审核按业务拆分为商户/管理员双角色。

**验证结果**：全部管理 Controller 已带权限注解；实测普通用户访问 `/api/admin/dashboard/stats` 返回 **HTTP 403**。

### 3.3 S3 优惠券越权使用（IDOR）

**问题描述**：下单时传入他人 `userCouponId` 可盗用其优惠券抵扣，属于不安全的直接对象引用。

**修复方案**：

```java
if (userCoupon != null && !userCoupon.getUserId().equals(userId)) {
    throw new BusinessException("优惠券不属于当前用户");
}
```

**验证结果**：`OrderServiceImpl.java:138` 已确认归属校验，跨用户券下单被拒绝。

### 3.4 S4 匿名文件上传 + 无类型白名单

**问题描述**：`/api/v1/files/**` 匿名放行且无扩展名白名单，可上传恶意 HTML/JS 造成存储型 XSS。

**修复方案**：
1. 接口改为 `authenticated()`；
2. `FileUploadController` 增加扩展名白名单（jpg/png/gif/webp 等）；
3. 静态资源 `/uploads/**` 显式放行保证图片可访问。

**验证结果**：未登录上传返回 401；非白名单扩展名被拒绝。

### 3.5 S5 敏感信息硬编码

**问题描述**：SMTP 授权码、JWT 密钥、MySQL 密码硬编码于配置文件。

**修复方案**：改为环境变量注入（`${MAIL_PASSWORD:xxx}`、`${JWT_SECRET:xxx}`），保留开发默认值便于本地运行。

**验证结果**：配置支持 `${ENV}` 注入；生产部署可覆盖默认值。

### 3.6 S6 密码重置验证码删除键错误

**问题描述**：写入 Redis 的键为 `email:code:{email}:reset`，删除时却使用 `email:code:{email}`，验证码永不被删除，可无限复用重置任意账号密码。

**修复方案**：删除键与写入键保持一致：

```java
redisTemplate.delete("email:code:" + request.getEmail() + ":reset");
```

**验证结果**：`UserController.java:266` 已确认按写入键格式修复。

### 3.7 S7 登录安全缺陷：日志泄露完整 JWT + 图形验证码可绕过

**问题描述**：
1. `UserServiceImpl.login` 中 `log.info("生成的完整 token: {}", token)` 将完整 JWT 写入日志，一旦日志泄露，攻击者可凭 token 冒充用户（账号会话劫持）。
2. 图形验证码仅在同时传入 `captchaKey` 与 `captchaCode` 时才校验，攻击者直接不传验证码即可绕过图形验证码，配合撞库/暴力破解登入他人账号。

**修复方案**：
1. 移除完整 token 打印，仅记录用户名与会话版本号：
```java
// 整改前
log.info("生成的完整 token: {}", token);
// 整改后
log.info("用户 {} 登录成功，会话版本: {}", user.getUsername(), sver);
```
2. 验证码改为必填强校验，校验通过后立即删除（一次性防重放）：
```java
// 验证图形验证码（A1 整改：必填强校验，防止不传验证码绕过）
if (!StringUtils.hasText(request.getCaptchaKey()) || !StringUtils.hasText(request.getCaptchaCode())) {
    throw new BusinessException("验证码不能为空");
}
String cacheKey = "captcha:" + request.getCaptchaKey();
String cachedCode = redisTemplate.opsForValue().get(cacheKey);
if (cachedCode == null) {
    throw new BusinessException("验证码已过期，请刷新");
}
if (!cachedCode.equalsIgnoreCase(request.getCaptchaCode())) {
    throw new BusinessException("验证码错误");
}
// 验证通过后删除，防止重复使用
redisTemplate.delete(cacheKey);
```
3. `LoginRequest` 的 `captchaKey` / `captchaCode` 增加 `@NotBlank` 注解兜底（`UserController.login` 已启用 `@Valid`）。

**验证结果**（`lisi/123456` 真实环境实测）：

| 场景 | HTTP | 响应 |
|------|------|------|
| 不携带验证码登录 | 400 | `验证码不能为空` |
| 验证码错误 | 400 | `验证码错误` |
| 正确验证码 + 正确凭据 | 200 | 返回 JWT，登录成功 |
| 复用同一验证码二次登录 | 400 | `验证码已过期，请刷新`（一次性防重放） |
| 正确验证码 + 错误密码 | 400 | `用户名或密码错误`（验证码校验不误判） |

前端唯一密码登录入口 `Login.vue` 已内置验证码组件（`fetchCaptcha()` 总会设置 `captchaKey`，表单 `captchaCode` 必填校验），无需改动。

---

## 4. 并发与数据一致性整改

> 共同根因：check-then-act（先查后改）竞态。统一解法：**条件 UPDATE 原子扣减**。

### 4.1 C1 下单扣库存非原子（超卖）

**问题描述**：`if (stock < qty) throw; setStock(stock - qty); updateById` 为读写分离操作，并发时两个事务同时读到库存=1 均通过校验，导致超卖。

**修复方案**：Mapper 层原子扣减，靠受影响行数判断：

```java
// UPDATE product_sku SET stock = stock - #{qty} WHERE id = #{id} AND stock >= #{qty}
if (productSkuMapper.deductStock(sku.getId(), qty) == 0) {
    throw new BusinessException("商品规格库存不足: " + product.getName());
}
```

**验证结果**：`OrderServiceImpl` 已确认使用原子 `deductStock`；商品维度同样处理。

### 4.2 C2 优惠券领取超发

**修复方案**：`couponMapper.deductStock(couponId)` 条件 UPDATE（`WHERE stock > 0`），返回 0 即抢完。

**验证结果**：`UserCouponServiceImpl.java:137` 已确认。

### 4.3 C3 秒杀 DB 库存丢更新

**修复方案**：`seckillSessionMapper.deductStock(sessionId)` 原子扣减。

**验证结果**：`SeckillServiceImpl.java:263` 已确认。

### 4.4 C4 定时任务自调用事务失效

**修复方案**：将 `@Transactional` 方法从定时入口拆离为独立调用路径，避免同 Class 自调用绕过 AOP 代理。

**验证结果**：调用链已确认修正。

---

## 5. 前端全局缺陷整改

### 5.1 F1 全屏 loading 永久泄漏

**问题描述**：`request.ts` 错误拦截器「无 response」分支与 blob 下载分支均未调用 `hideLoading()`，`requestCount` 只增不减，任一次网络超时/下载后全屏遮罩卡死。

**修复方案**：错误拦截器入口统一 `hideLoading()`；下载分支补 `hideLoading()`。

**验证结果**：代码已确认两个分支均关闭 loading。

### 5.2 F2 Axios 封装类型错位（TS2349）

**问题描述**：`RequestApi` 接口只有方法签名、无调用签名，导致全项目 `request({...})` 调用报 TS2349，并级联产生页面隐式 any 错误。

**修复方案**：为接口补充调用签名，与 `request()` 一致，支持 `request(config)` 与 `request.get(url)` 两种写法。

**验证结果**：全量 TS2349 报错清零，type-check 本项目页面 0 报错。

### 5.3 R1 我的优惠券页运行时白屏

**问题描述**：`MyCoupon.vue` 调用 `getMyCoupons` 但未导入，运行时报 `getMyCoupons is not defined`。

**修复方案**：补充导入 `import { getMyCoupons } from "@/api/eshop/user_coupons"`。

**验证结果**：构建通过，接口调用恢复正常。

---

## 6. 本次整改新增内容

> 在上一轮高危修复基础上，本次针对「仍可能引发生产事故」的隐患做了专项整改，全部通过编译与实测。

### 6.1 N1 mapper XML namespace 双重包名（9 个文件）

**问题描述**：mapper XML 的 `namespace` 写成 `com.shopsphere.eshop.mapper.com.shopsphere.eshop.mapper.XxxMapper`（包名重复拼接）。当前因未在 XML 中写自定义 SQL 而「侥幸」可启动，一旦添加自定义 SQL 将导致 MyBatis 启动绑定失败。

**修复方案**：9 个文件统一修正为 `com.shopsphere.eshop.mapper.XxxMapper`。

**涉及文件**：`AddressMapper.xml`、`CartMapper.xml`、`FavoriteMapper.xml`、`OrderMapper.xml`、`ProductCommentMapper.xml`、`ProductImageMapper.xml`、`ProductMapper.xml`、`UserMapper.xml`、`VisitLogMapper.xml`

**验证结果**：应用完整启动成功，Mapper 绑定正常，无启动异常。

### 6.2 N2 参数校验不完整

**问题描述**：下单数量 `quantity` 仅 `@NotNull` 无下限（传 0/负数破坏库存逻辑）；商品价格/库存无范围约束；嵌套 `OrderItemDTO` 因缺少 `@Valid` 级联校验实际不生效；`RefundApplyDTO` 误用 `org.jetbrains` 的 `@NotNull`（不参与 Jakarta Validation）；`CartController` 数量参数无校验。

**修复方案**：

| 位置 | 修复内容 |
|------|----------|
| `OrderCreateDTO` | `quantity` 增加 `@Min(1)`；`items` 增加 `@Valid` 级联 |
| `ProductSaveDTO` | `price` 增加 `@DecimalMin("0.01")`；`stock` 增加 `@Min(0)` |
| `RefundApplyDTO` | 注解改为 `jakarta.validation.constraints.NotNull` |
| `MerchantController` | `createProduct`/`updateProduct` 补充 `@Valid` |
| `CartController` | `quantity` 增加 `@Min(1)`（配合 `@Validated`） |
| `CartServiceImpl` | 服务层兜底校验，`quantity < 1` 抛业务异常（双保险，保证任何调用路径均生效） |

**验证结果**：单测 7 项全通过；接口实测 `quantity=0` 被拒绝、`quantity=2` 正常。

### 6.3 N3 越权访问返回 500 而非 403

**问题描述**：`AccessDeniedException`（`@PreAuthorize` 拒绝时抛出）被 `GlobalExceptionHandler` 的 `RuntimeException` 兜底处理成 HTTP 500「服务繁忙」，错误码语义错误，且前端无法区分「无权限」与「服务器故障」。

**修复方案**：新增专门异常处理器：

```java
@ExceptionHandler(AccessDeniedException.class)
public ResponseEntity<Result<?>> handleAccessDeniedException(AccessDeniedException ex) {
    log.warn("权限不足: {}", ex.getMessage());
    return buildResponse(403, "权限不足，无法访问该资源");
}
```

**验证结果**：实测普通用户访问管理接口由 **HTTP 500 修正为 HTTP 403**。

### 6.4 N4 补充最小业务测试集

**问题描述**：`src/test` 仅有一个空的 `contextLoads` 测试，无任何业务断言，无法证明修复有效性。

**修复方案**：新增 `ParamValidationTest`（7 个用例），直接使用 Jakarta Validator 验证 DTO 约束：
- 下单数量 = 0 或缺失 → 拒绝
- 商品价格 ≤ 0 → 拒绝
- 商品库存为负数 → 拒绝
- 合法订单/商品 → 通过
- 退款申请 `orderId` 为空 → 拒绝

**验证结果**：`Tests run: 7, Failures: 0, Errors: 0, Skipped: 0`（0.588s）。

### 6.5 第三阶段质量打磨（Q7 / Q9 / Q11 / Q12 / Q13）

> 在第一、二阶段高危项整改完成后，进一步推进《代码质量改进文档》第三阶段（质量打磨）的落地，本轮完成 5 项。

#### 6.5.1 Q7 秒杀状态魔数 → 常量

- **问题**：`SeckillController` 直接比较 `s.getStatus() == 0 || s.getStatus() == 1`，状态含义不明，且与已有常量类 `SeckillSessionStatus` 脱节。
- **修复**：改为 `SeckillSessionStatus.PENDING / ACTIVE` 常量引用。
- **验证**：编译通过。

#### 6.5.2 Q9 购物车列表 N+1 查询 → 批量查询

- **问题**：`CartServiceImpl.listCart` 对每件商品执行 2 次 `selectById`（商品 + SKU），购物车有 N 件商品即产生 1+2N 次 SQL。
- **修复**：先收集去重后的商品/SKU ID，各执行 1 次 `selectBatchIds`，内存组装（SQL 固定为 3 次）。
- **验证**：编译通过；行为与原逻辑一致（SKU 优先、缺省回退商品维度）。

#### 6.5.3 Q11 删除 TokenUtils 死代码

- **问题**：`TokenUtils` 中 `validateAuthorizationHeader` / `validateToken` / `maskToken` 三个私有方法从未被调用。
- **修复**：删除，类仅保留 `extractToken` / `extractTokenFromRequest`。
- **验证**：编译通过。

#### 6.5.4 Q12 复用 BCryptPasswordEncoder Bean

- **问题**：`UserServiceImpl`、`UserController` 各自 `new BCryptPasswordEncoder()`，与 `SecurityConfig` 已声明的 Bean 重复。
- **修复**：改为构造器注入 `SecurityConfig.passwordEncoder()` Bean，全系统单例复用。
- **验证**：编译通过，应用启动正常。

#### 6.5.5 Q13 操作日志 type 统一为常量

- **问题**：24 处 `@Log(type = "...")` 命名混乱，含拼写错误 `"Cancle_Order"`、风格不一的 `"Add_Coupon"`、`"ADD_SECKILL_SESSION"`、`"Change_Status"` 等。
- **修复**：新建 `OperationType` 常量类，24 处全部改为常量引用（如 `OperationType.CANCEL_ORDER`），统一为 UPPER_SNAKE_CASE。
- **涉及文件**：8 个 Controller（AdminCoupon、AdminRefund、AdminSeckill、Category、Notice、Order、Product、User）。
- **验证**：编译通过，无遗漏字符串 type。

### 6.6 A 组高危整改（A1 登录验证码 / A2 双弹提示 / A3 权限体系 / A4 UI Bug / A5 错误码体系 / A6 集成测试）

> 依据《整改待办清单》A 组（高危必做）推进，本轮完成 A1-A6，共 6 项。

#### 6.6.1 A1 登录验证码必填强校验

详见 3.7 节 S7（含代码与实测验证矩阵），本轮已同步完成。

#### 6.6.2 A2 错误提示双重弹出（F4）

- **问题**：响应拦截器已统一 `ElMessage.error` 提示业务错误，但部分页面 catch 分支又弹一次，出现双弹窗（登录页、商品详情、签到、优惠券、收藏、管理端日志/用户页等）。
- **修复**：约定「业务错误由请求拦截器统一提示，页面 catch 只处理逻辑」——清理 `product/detail.vue`（加载详情/加购×2/收藏/留言/评价/回复 7 处）、`SignIn.vue`（签到）、`Login.vue`（发送邮箱验证码/邮箱登录/密码登录 3 处，保留失败后刷新验证码）、`favorites/index.vue`（加购/取消收藏 2 处）、`eshop/log`、`eshop/user`（冻结/解冻/强制下线，保留用户取消确认框判断）；删除不再使用的 `ElMessage` import。
- **验证**：`eslint` 对全部改动文件通过；业务失败时仅拦截器弹一次提示。

#### 6.6.3 A3 权限体系自相矛盾（P5）

- **问题**：`utils/auth.ts` 的 `hasPerm()` 硬编码 `return true`（全放行），而 `v-hasPerm` 指令读取恒空 `perms: []`（全拦截），两者自相矛盾，按钮级权限实际失效。
- **修复**：
  1. `store/modules/user.ts`：登录拉取用户信息时按角色推导权限 `perms`（ADMIN → `["*:*:*"]` 通配，其余角色默认 `[]`）；
  2. `utils/auth.ts` `hasPerm()`：改为读取 store 权限做通配/精确匹配，删除硬编码 `return true`；
  3. `directives/permission/index.ts`：补 `perms.includes("*:*:*")` 通配放行；
  4. 删除死代码 `src/directives/hasPerm.ts`（未注册且错误地以 roles 充当权限）。
- **验证**：`eslint` 通过；管理员登录后 `v-hasPerm` 按钮可见，非 ADMIN 按权限隐藏（路由守卫亦拦截非 ADMIN 进入系统管理页）。

#### 6.6.4 A4 真实 UI Bug（D4，4 处）

- **问题**：前端 4 处可复现的真实 UI Bug：结算页类名多空格导致样式失效、商品图加载失败时把占位 URL 当文本渲染、优惠券面额三目恒真、商品详情数量上限用总库存而非 SKU 库存。
- **修复**：`checkout/index.vue` 类名 `coupon-selected-d etail` → `coupon-selected-detail`；`product/detail.vue` 图片 `#error` slot 渲染兜底图、新增 `maxStock` 计算属性（切 SKU 后上限跟随并钳制超限数量，`Math.max(1, stock)` 防御零库存）；`CouponCenter.vue` 三目恒真简化为直接输出。
- **验证**：`eslint` + `prettier` 通过；代码审查确认。

#### 6.6.5 A5 错误码体系落地（Q8）

- **问题**：业务失败全部 `return Result.error("...")`（固定 code=500），无法区分参数错误（4xx）与服务器错误（5xx）；`ErrorCode` 枚举已定义但未使用。
- **修复**：
  1. 7 个 Controller 共 26 处 `Result.error` 改为 `throw new BusinessException("...")`（默认 `ErrorCode.BAD_REQUEST` → HTTP 400），由 `GlobalExceptionHandler.handleBusinessException` 统一返回 `{code, msg}` 与正确 HTTP 状态；文件上传 `IOException` 使用 `ErrorCode.INTERNAL_ERROR`（500）；
  2. 补充 `NoResourceFoundException` 处理器（Spring Boot 3.2 未匹配路径统一抛该异常），由兜底 500 改为 HTTP 404「请求的接口不存在」；
  3. `AccessDeniedException → 403`（越权）沿用已先行完成的处理器。
- **涉及文件**：`AdminMerchantApplyController`、`FileUploadController`、`EmailAuthController`、`MerchantController`、`UserController`、`StoreController`、`MerchantMessageController`、`GlobalExceptionHandler`。
- **验证**（`lisi/123456` 登录后实测，见 7.2 表 #6-#11）：参数错误 400、空文件 400、商家不存在 400、留言为空 400、接口不存在 404、越权/未登录 403 全部符合预期。

#### 6.6.6 A6 安全 / 并发 / 越权集成测试补充

- **背景**：项目此前仅有 1 个启动冒烟测试与 7 个参数校验单测，安全与并发正确性（整改项 S1-S8、C1/C2）缺少自动化回归保障。
- **修复**：新增两个 `@SpringBootTest` 集成测试类，共 12 个用例：
  - `A6SecurityIntegrationTest`（8 个，MockMvc 走真实 HTTP 链路，含 Spring Security / 全局异常处理 / Redis 验证码）：
    1. 普通用户访问 `/api/user/admin/page` → 403「权限不足，无法访问该资源」（S1/S2）；
    2. 管理员访问同接口 → 200（S1/S2）；
    3. 未登录上传 `/api/v1/files` → 403（Security 入口点，S4）；
    4. `.html` 文件被白名单拒绝 → 400「不支持的文件类型…」（S4）；
    5. 空文件上传 → 400「上传文件不能为空」（S4）；
    6. 未匹配路径 `/api/not-exist` → 404「请求的接口不存在」（A5）；
    7. 重置密码验证码错误 → 400「验证码错误或已过期」（S6）；
    8. 验证码一次性：同码二次登录 → 400「验证码已过期，请刷新」（A1）。
  - `A6BusinessIntegrationTest`（4 个，Service 层 + 并发）：
    1. 并发 20 线程抢库存 10 的商品 → 成功数 ≤ 10 且剩余库存精确 = 10 − 成功数（不超卖，C1）；
    2. 并发 20 线程领取库存 5 的券 → 成功数 ≤ 5 且发放记录数 = 成功数（不超发，C2）；
    3. 用户 A 使用用户 B 的券下单 → 抛「优惠券不属于当前用户」且事务回滚、库存未扣（S3）；
    4. 已注销（`deleted=1`）用户不出现在搜索列表（S8）。
- **测试数据治理**：测试数据带 `A6T{8位UUID}` 唯一标记，`@AfterEach` 用 `JdbcTemplate` 按级联顺序（`sys_notice → order_item → order_shipment → order → product → user_coupon → coupon → user`）物理清理，不污染开发库；JWT 签发 `sver=999_999_999` 避免与其它用例的会话版本递增产生竞态。
- **验证**：`mvn test` 全量 20 个用例全部通过（见 7.1）。

---

## 7. 测试与验证结果

### 7.1 单元 / 集成测试

`mvn test` 全量 20 个用例全部通过（Failures: 0, Errors: 0）：

```
Test set: com.shopsphere.eshop.EShopApplicationTests
Tests run: 1, Failures: 0, Errors: 0, Skipped: 0

Test set: com.shopsphere.eshop.ParamValidationTest
Tests run: 7, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.178 s

Test set: com.shopsphere.eshop.A6SecurityIntegrationTest      （A6 新增：安全/越权/错误码）
Tests run: 8, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 6.253 s

Test set: com.shopsphere.eshop.A6BusinessIntegrationTest      （A6 新增：并发不超卖/不超发/券越权/注销搜索）
Tests run: 4, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 17.75 s
```

> 说明：A6 集成测试为 `@SpringBootTest`（MOCK 环境，不占用 8080 端口），覆盖整改项 S1/S2/S4/S6/S8、C1/C2、A1/A5，测试数据自动物理清理。

### 7.2 接口实测（真实环境，dev profile）

| # | 验证场景 | 预期 | 实测结果 | 状态 |
|---|----------|------|----------|------|
| 1 | 后端启动 | 正常启动 | `Started EShopApplication in 9.5s`，Tomcat 8080 | ✅ |
| 2 | 登录 `/api/user/login` | 200 + JWT | code=200 | ✅ |
| 3 | 普通用户访问 `/api/admin/dashboard/stats` | 403 | **HTTP 403**（整改前 500） | ✅ |
| 4 | 购物车添加 `quantity=0` | 拒绝 | 拒绝（HTTP 400） | ✅ |
| 5 | 购物车添加 `quantity=2` | 成功 | code=200 | ✅ |
| 6 | 重置密码验证码错误 | 400 | HTTP 400「验证码错误或已过期」 | ✅ |
| 7 | 空文件上传 `/api/v1/files` | 400 | HTTP 400「上传文件不能为空」 | ✅ |
| 8 | 访问不存在商家 `/api/merchant/999999/store` | 400 | HTTP 400「商家不存在」 | ✅ |
| 9 | 留言内容为空 `/api/merchant-message` | 400 | HTTP 400「请输入留言内容」 | ✅ |
| 10 | 已登录访问不存在路径 | 404 | HTTP 404「请求的接口不存在」（整改前 500 兜底） | ✅ |
| 11 | 未登录访问受保护接口 / 越权访问 | 403 | HTTP 403（Security 入口点 / AccessDenied 处理器） | ✅ |

### 7.3 构建验证

- 后端：`mvn spring-boot:run` 编译启动通过；`mvn test` 通过。
- 前端（上轮完成）：`vite build` 成功（3101 模块）；type-check 本项目页面 0 报错。

---

## 8. 整改前后对比

| 维度 | 整改前 | 整改后 |
|------|--------|--------|
| 权限体系 | 注解失效 + 管理接口裸奔 | 方法级 + URL 双层防护，403 语义正确 |
| 越权防护 | 优惠券可盗用 | 归属校验 + 服务层兜底 |
| 并发正确性 | 先查后改，可超卖/超发 | 原子条件 UPDATE，行数判断 |
| 输入校验 | 数量可为 0/负数，嵌套校验失效 | DTO + 服务层双层校验，单测覆盖 |
| 敏感信息 | 密钥硬编码 | 环境变量注入 |
| 前端稳定性 | loading 泄漏、类型全量报错、页面白屏 | 全部修复，构建通过 |
| 测试覆盖 | 空测试 | 20 个自动化用例（1 冒烟 + 7 参数校验 + 12 集成），全绿 |

---

## 9. 遗留问题与后续计划

以下项不影响系统运行与构建，属质量打磨，已列入后续迭代：

1. **前端类型（模板既有）**：`Upload/*.vue` 的 `defineModel` 类型推断、vxe-table `DefaultRow` 与业务类型不匹配（不影响 `vite build`）。
2. **代码重复**：默认图兜底、商品/订单/优惠券卡片建议抽取公共组件。
3. **性能优化**：订单 VO 组装 N+1 查询（每单 5+ 次查询），建议 `selectBatchIds` 批量 + 内存组装（购物车维度已整改）。
4. **token 提取重复**：40+ 处手动解析，建议统一 `@AuthenticationPrincipal`。
5. **测试扩展**：补充并发下单（`deductStock` 竞态）、优惠券归属、密码重置验证码复用的集成测试。
6. **密钥默认值**：正式部署前移除配置文件内开发默认值，全部改为外部注入。
7. **错误码体系**：`ErrorCode` 已定义但未落地，`Result.error` 仍固定 500，建议区分 4xx/5xx 并统一错误码（`AccessDeniedException → 403` 已先行修复）。

---

## 10. 结论

本次整改共解决 **26 项问题（高危 13 项）**，覆盖权限体系、越权防护、并发正确性、输入校验、日志安全、前端稳定性、测试覆盖与代码质量八大方面。整改后系统通过了单元测试（7/7）、接口实测（登录/越权/参数校验）与完整启动验证，功能正常、风险可控，可支撑毕业设计的「系统测试」章节及答辩演示。

---

*报告结束。*
