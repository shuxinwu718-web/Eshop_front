# Eshop 电商系统 — 整改待办清单

> 版本：v1.0 ｜ 创建：2026-08-07 ｜ 说明：汇总《代码质量改进文档》中全部**未完成**条目，按优先级分组，每项含位置、方案与验证方式，可逐条勾选排查。
> 已完成项（S1-S6/S8、C1-C4、F1-F3、Q4-Q7/Q9(部分)/Q11-Q13、S7 日志部分）不在本清单内。

---

## A 组：高危必做（答辩必问 / 影响演示观感）

### A1. S7-2 登录验证码可绕过（安全必问） ✅ 已完成 2026-08-07

- [x] **位置**：`UserServiceImpl.login`（约 92 行 `if (hasText(key) && hasText(code))`）
- [x] **问题**：验证码仅在同时传了 key+code 才校验，不传即绕过图形验证码。
- [x] **方案**：改为必填强校验（key/code 任一为空即抛「验证码不能为空」）；校验通过后立即删除 Redis 验证码（一次性），防止重放；`LoginRequest.captchaKey/captchaCode` 加 `@NotBlank` 兜底。前端唯一密码登录入口 `Login.vue` 已带验证码组件（`fetchCaptcha` 总会设置 captchaKey，表单 `captchaCode` 必填），无需改动。
- [x] **验证**（`lisi/123456` 实测）：
  - 不带验证码 → HTTP 400「验证码不能为空」✅
  - 验证码错误 → 「验证码错误」✅
  - 正确验证码 + 正确凭据 → HTTP 200 返回 JWT ✅
  - 复用同一验证码二次登录 → 「验证码已过期，请刷新」（一次性防重放）✅
  - 正确验证码 + 错误密码 → 「用户名或密码错误」（验证码校验不误判）✅

### A2. F4 错误提示双重弹出 ✅ 已完成 2026-08-07

- [x] **位置**：`src/views/shop/checkout/index.vue:322`、`product/detail.vue:407/418/485`、`SignIn.vue:234`、`CouponCenter.vue:250/321`
- [x] **问题**：拦截器已 `ElMessage.error`，页面 catch 又弹一次。
- [x] **方案**（采用方案①）：约定业务错误由请求拦截器统一提示、页面 catch 只处理逻辑。清理 `product/detail.vue`（加载详情/加购×2/收藏/留言/评价/回复 7 处）、`SignIn.vue`（签到）、`Login.vue`（发送邮箱验证码/邮箱登录/密码登录 3 处，保留失败后刷新验证码逻辑）、`favorites/index.vue`（加购/取消收藏 2 处）、`eshop/log`、`eshop/user`（冻结/解冻/强制下线，保留用户取消确认框判断）；删除 `eshop/log` 不再使用的 `ElMessage` import。
- [x] **验证**：`eslint` 对全部改动文件通过；制造业务失败（如余额不足/重复领取）由拦截器统一弹一次，不再双弹。

### A3. P5 权限体系自相矛盾 ✅ 已完成 2026-08-07

- [x] **位置**：`src/utils/auth.ts:39-41`（`hasPerm()` 硬编码 `return true`）、`src/directives/permission.ts`（读恒空 `perms: []`）
- [x] **问题**：一个全放行、一个全拦截，按钮级权限逻辑实际失效且互相矛盾。
- [x] **方案**：统一为 store 内动态权限——`store/modules/user.ts` 登录拉取用户信息时按角色推导 `perms`（ADMIN → `["*:*:*"]` 通配，其余角色默认 `[]`）；`utils/auth.ts` `hasPerm()` 改为读取 store 权限（通配/精确匹配）；`directives/permission/index.ts` 补 `perms.includes("*:*:*")` 通配放行；删除死代码 `src/directives/hasPerm.ts`（未注册且读 roles 当权限用）。
- [x] **验证**：`eslint` 通过；管理员登录后 `v-hasPerm` 按钮可见，普通用户登录系统管理页按钮按 `perms` 隐藏（路由守卫本就拦截非 ADMIN 进入）。

### A4. D4 真实 UI Bug（4 处，低成本高回报） ✅ 已完成 2026-08-07

- [x] `checkout/index.vue:79`：类名 `coupon-selected-d etail` 中间多空格 → 修复为单空格，`.coupon-selected-detail` 样式生效。
- [x] `product/detail.vue:25`：图片加载失败时把占位 URL 当文本渲染 → `#error` slot 改为渲染兜底图 `<img :src="defaultImage">`。
- [x] `CouponCenter.vue:128`：恒真三目 `item.type === 0 ? item.value : item.value` → 直接输出 `{{ item.value }}`。
- [x] `product/detail.vue:101`：数量上限用商品总库存 → 新增 `maxStock` 计算属性（有 SKU 时用 SKU 库存），切换 SKU 后上限跟随且自动钳制超限数量，`Math.max(1, stock)` 防御零库存。
- [x] **验证**：`eslint` 通过；类名/图片兜底/数量上限逻辑代码审查确认。

### A5. Q8 错误码体系落地 ✅ 已完成 2026-08-07

- [x] **位置**：`common/Result.java`、`exception/ErrorCode.java`（已定义未用）、`exception/GlobalExceptionHandler.java`、各 Controller 的 `return Result.error("...")`
- [x] **问题**：`Result.error` 固定 code=500，未区分 4xx/5xx，错误码体系未落地。
- [x] **方案**：① `Result` 带 HTTP 状态/业务码的 error 重载已具备（`error(int,msg)`）；② 7 个 Controller 共 26 处 `return Result.error("...")` 改为 `throw new BusinessException("...")`（默认 400），由 `GlobalExceptionHandler.handleBusinessException` 统一设置 HTTP 状态；文件上传 `IOException` 用 `ErrorCode.INTERNAL_ERROR`（500）；③ `ErrorCode` 枚举（400/401/403/404/409/500）收敛业务码；额外修复：Spring Boot 3.2 `NoResourceFoundException`（未匹配路径）从兜底 500 改为返回 404「请求的接口不存在」。
- [x] **涉及文件**：`AdminMerchantApplyController`（审核状态）、`FileUploadController`（空文件/类型白名单/IO 异常）、`EmailAuthController`（验证码/未注册/冻结）、`MerchantController`（商品不存在×4/状态值/快递公司/快递单号/回复内容）、`UserController`（用户不存在×4/原密码/密码/验证码/未注册）、`StoreController`（商家不存在）、`MerchantMessageController`（留言为空）、`GlobalExceptionHandler`（+NoResourceFoundException→404）。
- [x] **验证**（`lisi/123456` 登录后实测）：
  - 业务参数错误（重置密码验证码错）→ HTTP 400「验证码错误或已过期」✅
  - 空文件上传 → HTTP 400「上传文件不能为空」✅
  - 商家不存在 → HTTP 400「商家不存在」✅
  - 留言内容为空 → HTTP 400「请输入留言内容」✅
  - 已登录访问不存在路径 → HTTP 404「请求的接口不存在」✅
  - 未登录访问受保护接口 → HTTP 403（Security 默认入口点）；越权访问 → HTTP 403（`AccessDeniedException` 处理器）✅

### A6. 集成测试补充（答辩核心加分） ✅ 已完成 2026-08-07

- [x] **位置**：`src/test/java/com/shopsphere/eshop/`（新增 `A6SecurityIntegrationTest`、`A6BusinessIntegrationTest`）
- [x] **方案**：两个 `@SpringBootTest` 集成测试类，MockMvc 走真实 HTTP 链路（含 Spring Security / 全局异常处理 / Redis 验证码），业务规则走 Service 层；测试数据带 `A6T{8位UUID}` 唯一标记，`@AfterEach` 用 JdbcTemplate 按级联顺序物理清理，不污染开发库；JWT 用 `jwtUtil.generateToken(id, username, role, 999_999_999L)` 签发超大 sver 避免会话版本竞态。
- [x] **用例清单**（12 个，全绿）：
  - `A6SecurityIntegrationTest`（8 个）：普通用户访问 `/admin/**` → 403（S1/S2）；管理员 → 200；未登录上传 → 403；`.html` 白名单外 → 400（S4）；空文件 → 400；未匹配路径 → 404（A5）；重置密码验证码错 → 400（S6）；验证码一次性——同码二次登录 → 400「验证码已过期，请刷新」（A1）
  - `A6BusinessIntegrationTest`（4 个）：并发 20 单抢库存 10 → 成功数 ≤ 10 且剩余库存精确 = 10-成功数（C1）；并发 20 次领券库存 5 → 成功数 ≤ 5 且发放记录数一致（C2）；用户 A 用用户 B 的券下单 → 「优惠券不属于当前用户」且事务回滚库存未扣（S3）；已注销（deleted=1）用户不出现在搜索结果（S8）
- [x] **验证**：`mvn test -Dtest=A6SecurityIntegrationTest,A6BusinessIntegrationTest` → Tests run: 12, Failures: 0, Errors: 0；`mvn test` 全量（含 `EShopApplicationTests`、`ParamValidationTest`）全部通过。

---

## B 组：中危（质量与性能）

### B1. Q1 Controller 手动提取 token 重复 40+ 处

- [ ] **位置**：OrderController / UserController / CartController 等（`tokenUtils.extractToken` + `jwtUtil.getUserIdFromToken` 模式）
- [ ] **方案**：引入 `@AuthenticationPrincipal`（登录时已存 SecurityContext）或自定义 `@CurrentUserId` ArgumentResolver，删除重复提取代码。
- [ ] **验证**：编译通过 + 抽 2~3 个接口回归。

### B2. Q9 剩余 N+1（订单/通知维度）

- [ ] **位置**：`OrderServiceImpl.convertToOrderVO`（每单 5+ 次 selectById）、`NoticeServiceImpl`（每通知 1 次 selectCount）
- [ ] **方案**：仿照 `CartServiceImpl` 已改模式——先收集 ID 集合，`selectBatchIds` 批量查询 + 内存 Map 组装。
- [ ] **验证**：打开订单列表，控制台 SQL 日志由 1+5N 降为固定 N 次。

### B3. Q3 SKU 库存/销量汇总同步复制 4 份

- [ ] **位置**：`OrderServiceImpl` 246/350/848、`ProductServiceImpl:417`
- [ ] **方案**：抽取 `syncSkuStockAndSales(Long productId)` 工具方法统一调用。
- [ ] **验证**：下单/退款/取消/删除订单后 SKU 汇总数据一致。

### B4. Q2 分页查询模板重复

- [ ] **位置**：`OrderServiceImpl`（518/673/652 行）`pageQuery`/`userPageQuery`/`adminPageQuery`
- [ ] **方案**：抽通用分页方法（入参 + 条件构造器 + 转换器），消除三份模板。
- [ ] **验证**：三处列表接口行为不变，编译通过。

### B5. Q10 吞异常 / 假实现

- [ ] **位置**：`SeckillServiceImpl.autoUpdateStatus`（吞异常）、`UserController:175/182`（短信 TODO 假实现返回成功）
- [ ] **方案**：吞异常处记录日志并评估是否回滚；短信假实现改为明确提示「短信服务未接入」或移除假成功返回。
- [ ] **验证**：触发相应路径，日志有据可查、返回提示真实。

### B6. C5 类级 @Transactional 过宽

- [ ] **位置**：`NoticeServiceImpl`、`MerchantApplyServiceImpl`（类级注解）
- [ ] **方案**：只读方法去掉事务或改 `@Transactional(readOnly = true)`。
- [ ] **验证**：编译通过，相关读写接口回归。

### B7. P1 列表 key 冲突

- [ ] **位置**：`order/list.vue:37`、`center/index.vue:67`、`checkout/index.vue:39`（`:key="item.productId"`）
- [ ] **方案**：同订单同商品不同 SKU 时 key 冲突，改为 `item.id` 或 `productId + '-' + skuId`。
- [ ] **验证**：购物车含同商品两规格时，Vue 无 key 重复警告。

### B8. P3 倒计时 1s 定时器触发整页刷新

- [ ] **位置**：`order/list.vue:451-460`、`seckill/index.vue:176-179`
- [ ] **方案**：倒计时 1s 驱动但刷新间隔降频（10-30s），过期检测单独处理；后台刷新跳过全屏 loading。
- [ ] **验证**：待付款/秒杀页运行 1 分钟，网络请求频率正常。

### B9. P4 onMounted 多请求无错误隔离

- [ ] **位置**：`home/index.vue:279-283`（`loadCategories` 无 try/catch）、`center/index.vue:592-595`（串行 await）
- [ ] **方案**：`Promise.allSettled` + 逐请求 try/catch 隔离。
- [ ] **验证**：断开单个接口，其余区块正常渲染。

### B10. P6 导航守卫 catch 一刀切踢登录

- [ ] **位置**：`router/guards/permission.ts:98-102`
- [ ] **方案**：网络错误与 401 区分——401 才重置并跳登录，网络错误提示后停留。
- [ ] **验证**：断网刷新不强制跳登录页。

### B11. D5 user store 三份用户信息副本

- [ ] **位置**：`store/modules/user.ts`（内存 `userInfo` + localStorage `userInfo` + `user_info_cache`）
- [ ] **方案**：收敛为「内存 + 一份带过期缓存」，统一清理时机。
- [ ] **验证**：修改昵称/头像后刷新页面，各处显示一致。

---

## C 组：低危 / 打磨（量力而行）

### C1. Q15 日志 emoji、import 清理

- [ ] **位置**：`JwtAuthenticationFilter:106`（日志含 emoji）、`OrderServiceImpl:12`（import 混乱）
- [ ] **方案**：移除日志 emoji；按需清理未使用 import。
- [ ] **验证**：编译通过。

### C2. Q14 超长类/方法拆分（工作量最大）

- [ ] **位置**：`OrderServiceImpl`（1022 行）、`createOrder`（222 行）、`MerchantController`（449 行，6 类职责）
- [ ] **方案**：按业务拆分——如 `OrderServiceImpl` 拆出 `OrderQueryService` / `OrderCancelService` / `OrderPaymentService`；`MerchantController` 拆出商品/订单/申请子控制器。
- [ ] **验证**：功能回归 + 编译通过（建议最后做，风险最高）。

### C3. D1 抽取公共组件

- [ ] `useImageFallback.ts` + 默认图常量（home/cart/checkout/favorites/order-list/detail/center 共用）
- [ ] `components/Shop/ProductCard.vue`（商品卡片：img+名称+价格+跳详情）
- [ ] `components/Shop/OrderCard.vue` + `OrderStatusTag`（订单卡片：状态/支付/取消/收货）
- [ ] `components/Shop/CouponCard.vue`（优惠券渲染）
- [ ] `components/Shop/FestivalClaimButton.vue`（节日券领取）
- [ ] **验证**：替换后对应页面视觉与交互一致。

### C4. D2 center/index.vue 巨型聚合页拆分

- [ ] **位置**：`center/index.vue`（约 860 行，6 大区块 v-show 内嵌）
- [ ] **方案**：区块改路由跳转（`/shop/order`、`/favorites`、`/shop/address`），或按区块拆子组件。
- [ ] **验证**：个人中心各入口跳转与功能正常。

### C5. D3 超大组件拆分（7 个）

- [ ] `product/detail.vue`（1048 行）→ SKU 选择器/评论树/尺寸表/联系商家弹窗拆组件
- [ ] `ProductForm.vue`（988 行）→ 尺寸表编辑器/SKU 笛卡尔积生成器独立
- [ ] `order/list.vue`（795 行）→ 支付/退款/进度/满意度 4 弹窗独立
- [ ] `CouponCenter.vue`（731 行）→ 节日券专区与领券中心拆分
- [ ] **验证**：拆分后各页功能回归。

### C6. P2 图片懒加载

- [ ] **位置**：home/favorites/center/detail 等商品图
- [ ] **方案**：`<img loading="lazy">` 或 `el-image :lazy="true"`。
- [ ] **验证**：页面滚动加载，Network 可见按需请求。

### C7. F5 any 泛滥（116 处）收敛

- [ ] **位置**：`api/eshop/merchant.ts:144/148`、`ProductForm.vue`（`ref<any[]>`）、`request.ts:14` 等
- [ ] **方案**：eshop 类型收敛到 `src/types/api`；表单/上传对象补类型。
- [ ] **验证**：`vue-tsc` 报错数下降且无新增。

### C8. 第 4 阶段：DTO/VO 与前端类型同步

- [ ] **方案**：引入 OpenAPI 生成（springdoc 输出 → `openapi-typescript` 生成前端类型），或先建立人工同步清单。
- [ ] **验证**：后端新增字段后前端类型可自动更新，无手工漂移。

---

## 建议排查顺序（按答辩价值排序）

1. A 组全部（高危，影响答辩演示与必问项）
2. B1（token 统一注入，代码整洁度高）→ B2（性能，可测）→ B3/B4/B5/B6
3. B7-B11（前端稳定与一致性）
4. C 组（组件化与结构优化，工作量大、风险高，最后做）

> 完成后逐条勾选，并在《代码质量改进文档》中同步标记 ✅。
